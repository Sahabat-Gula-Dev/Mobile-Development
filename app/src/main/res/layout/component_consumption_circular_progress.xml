<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="221dp"
    android:layout_height="221dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:progress="100"
        app:indicatorColor="#FFDDE8"
        app:indicatorSize="221dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:trackColor="#FFDDE8"
        app:trackThickness="16dp" />

    <!-- progress circle -->
    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/pi_circular_progress"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:max="100"
        android:progress="0"
        app:indicatorColor="#FF3776"
        app:indicatorSize="221dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:trackColor="#FFDDE8"
        app:trackCornerRadius="100dp"
        app:trackThickness="16dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="@id/pi_circular_progress"
        app:layout_constraintEnd_toEndOf="@id/pi_circular_progress"
        app:layout_constraintStart_toStartOf="@id/pi_circular_progress"
        app:layout_constraintTop_toTopOf="@id/pi_circular_progress">

        <ImageView
            android:id="@+id/ic_circular_progress"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:src="@drawable/ic_sugar_candy_unfilled"/>
        
        <TextView
            android:id="@+id/tv_remaining"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="47"
            android:gravity="center"
            android:textColor="#FF3776"
            android:textSize="32sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_format"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:letterSpacing="0.03"
            android:text="gram tersisa"
            android:textSize="12sp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>

